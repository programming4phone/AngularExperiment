import { RefsDirective } from './refs.directive';

describe('RefsDirective', () => {
  it('should create an instance', () => {
    // const directive = new RefsDirective();
    // expect(directive).toBeTruthy();
  });
});
